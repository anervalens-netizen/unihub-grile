# Decisions

This folder captures the architectural and product decisions made at each
stage. The format follows the active ExecPlan's "Decisions" section, but the
Markdown files here are reviewable without opening the tracker.

* `s1-stack.md` — Stage 1 stack pinning, AC-02 enforcement layers, auth
  skeleton and connector strategy.

Future stages will add their own files when they introduce choices that
deserve explicit documentation.